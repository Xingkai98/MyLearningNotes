一、Query、Key、Value 到底是什么
Query是你心里想问的问题。Key是字典里每个词的名字。Value是字典里每个词对应的实际数值。Query和Key做匹配，得到注意力权重，再用权重去加权 Value，这就是注意力机制的本质。

二、公式推导过程
第一步是用点积找出相似度，用点积衡量Query和Key像不像。因为词向量有个特性：语义相近的词，向量点积更大。
第二步是转成概率分布。光有点积不行，数值有大有小。扔给Softmax，输出的东西相加等于1，正好表示注意力权重。
第三步是加权求和。用注意力权重去加权Value。
第四步是缩放一下。当 dk（Key 的维度）比较大的时候，QK^T 的值会偏大，Softmax容易走到极端区域，梯度不稳定。解决方法是除以根号dk。
最终公式就是 attention(Q, K, V) = softmax(QK^T / √dk) × V

三、自注意力
普通的注意力是“两个序列之间”的注意力。但Transformer的Encoder用的是自注意力——Query、Key、Value都来自同一个序列。说白了就是：我这句话里的每个词，跟这句话里其他词的关系是什么样的。

四、掩码自注意力
训练的时候，语言模型要根据前文预测下一个词。正常做法是串行的——先算第一个，再算第二个，再算第三个。这样太慢了，没利用上 GPU 的并行能力。
掩码的思路是：既然第 4 个位置不该看到第 5、6、7... 个词，那就在注意力计算时把这些位置遮住。用-inf来遮被mask住的位置，注意力分数变成 0，相当于模型假装那些 token 不存在。这样就能一次把整个序列扔进模型，同时学预测所有位置的下一个词。

五、多头注意力
单一注意力只能学到一种关系。但一句话里可能同时存在主语和谓语的关系、指代关系、语义相似关系、语法结构关系等多种关系。多头注意力就是同时运行多组参数，每组学一种关系，最后把结果拼起来。

六、整体回顾
注意力机制要解决的核心问题是：如何让模型在处理一个序列时，知道序列里哪些部分跟哪些部分有关系，关系有多强。RNN 靠"依次读取"来解决，但慢，且长距离关系容易丢。注意力机制靠"并行查询"来解决，快，且能直接建模任意距离的关系。难怪它能取代 RNN 成为 NLP 的主流架构。