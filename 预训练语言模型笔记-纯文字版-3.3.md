# 第三章 预训练语言模型

## 一、Decoder-Only——LLM 的基础架构

Decoder-Only 是目前大火的 LLM 的基础架构。ChatGPT 就是 GPT 系列的大成之作，LLaMA 等主流开源 LLM 也是在 GPT 架构基础上发展而来。Decoder-Only 天生适合文本生成任务。

## 二、GPT——打开 LLM 大门的钥匙

**（1）模型架构——Decoder Only**

GPT 只用了 Transformer 的 Decoder 部分。输入文本先通过 tokenizer 转成 token id，再经过 Embedding 层和位置编码，然后过 N 层 Decoder，最后通过线性层映射到词表维度输出。

GPT 和 BERT 的主要区别：BERT 用 Encoder，GPT 用 Decoder。Decoder 有掩码自注意力，每个 token 只能看到它之前的 token，所以天然适合生成任务。GPT 用的是 Pre-Norm（先 LayerNorm 再注意力），位置编码用的是和 Transformer 一样的 Sinusoidal 绝对位置编码。

GPT-1 有 12 层 Decoder，768 隐藏层维度，12 个注意力头，参数量 1.17 亿。

**（2）预训练任务——CLM**

CLM 是因果语言模型，就是根据前 N 个 token 预测下一个 token。不断重复这个过程生成整个序列。这和人类写作的方式一致，所以下游任务匹配度最高。

CLM 比 MLM 更直接，可以在任何自然语言文本上直接训练，所以也是最早被广泛使用的预训练任务。

**（3）GPT 系列的发展**

GPT-1：12 层，768 维，5GB 预训练数据（BookCorpus），参数量 0.12B。发布时效果不如 BERT，没引起轰动。

GPT-2：48 层，1600 维，40GB 预训练数据（WebText），参数量 1.5B。提出了 zero-shot 思路，不微调直接做任务。但当时模型能力还不够，zero-shot 效果一般。

GPT-3：96 层，49152 维，570GB 预训练数据，参数量 175B。使用稀疏注意力机制，提出了 few-shot 思想，在 prompt 里给 3~5 个示例让模型学习。few-shot 被称为上下文学习（In-Context Learning），是 LLM 的核心优势之一。GPT-3 标志着 LLM 时代的开启。

## 三、LLaMA——开源 LLM 的基石

**（1）模型架构——Decoder Only**

LLaMA 也是 Decoder-Only 结构，和 GPT 类似。处理流程是：tokenizer 编码 → Embedding 层 → 位置编码 → N 层 Decoder Block → 线性层输出词表概率。

Decoder Block 内部：先 masked self-attention（计算 query、key、value），再过 MLP（两个全连接层）。

**（2）LLaMA 系列发展**

LLaMA-1（2023年2月）：7B、13B、30B、65B 四个版本，在超过 1T token 上训练。65B 版本在 2048 张 A100 上训练了近 21 天。开源后迅速成为最受欢迎的开源大模型。

LLaMA-2（2023年7月）：7B、13B、34B、70B 版本，预训练语料扩充到 2T token，上下文长度从 2048 翻倍到 4096。引入了分组查询注意力机制（GQA）。

LLaMA-3（2024年4月）：8B 和 70B 版本，支持 8K 长文本。词表扩大到 128K，使用了超过 15T token 的预训练语料（是 LLaMA-2 的 7 倍多）。

## 四、GLM——国产 LLM 的代表

**（1）模型架构——相对于 GPT 的修正**

GLM 和 GPT 大致类似，但有三处细微差异：

一是用 Post Norm 而非 Pre Norm。Post Norm 是先残差计算再 LayerNorm，Pre Norm 则相反。GLM 认为 Post Norm 可以避免 LLM 的数值错误。

二是用单个线性层做最终 token 预测，而不是 MLP，减少参数量。

三是激活函数从 ReLU 换成 GeLU。GeLU 对接近 0 的正向传播做非线性映射，有一定连续性。

**（2）预训练任务——GLM**

GLM 的核心创新是结合了 MLM 和 CLM 的思想。自编码思想就是在输入中随机删除连续 tokens，像 MLM 一样学习。自回归思想是像 CLM 一样按顺序重建。

GLM 的做法是：随机掩码不是单个 token，而是一段连续的 tokens。模型既要根据上下文预测这段内容，在这段内部又要按顺序预测每个 token。这样既适配生成任务，又迫使模型从前后两个方向学习。

不过 GLM 的优势在预训练模型时代更明显。进入 LLM 时代后，CLM 在 scale 效应下表现更好，所以 ChatGLM2 之后也回归了传统的 CLM。

**（3）GLM 家族发展**

ChatGLM-6B（2023年3月）：第一个中文开源 LLM，在 1T 语料上预训练，支持 2K 上下文。

ChatGLM2-6B（2023年6月）：上下文扩展到 32K，架构基本回归 LLaMA，引入了 MQA 注意力机制，预训练任务回归 CLM。

ChatGLM3-6B（2023年10月）：在语义、数学、推理、代码和知识方面达到当时 SOTA。开始支持函数调用与代码解释器。

GLM-4（2024年1月）：支持 128K 上下文，评估达到 GPT-4 水平。只开源了轻量级版本 GLM-4-9B。

## 五、总结

GPT 系列证明了 Decoder-Only 架构和 scale 策略的正确性，GPT-3 开启了 LLM 时代。LLaMA 通过开源推动了 LLM 的普及和发展。GLM 在融合 MLM 和 CLM 方面做了有益探索，ChatGLM 系列也成为中文 LLM 的重要力量。Decoder-Only 架构凭借其简单性和在生成任务上的优势，成为当今 LLM 的主流选择。