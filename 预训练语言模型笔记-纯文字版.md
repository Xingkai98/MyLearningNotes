# 第三章 预训练语言模型

## 一、Encoder-Only 三剑客

BERT、RoBERTa、ALBERT 这三个模型都是只用了 Transformer 的 Encoder 部分，所以叫 Encoder-Only。它们的核心目标都是做自然语言理解（NLU），比如文本分类、情感判断。

## 二、BERT——划时代的模型

**（1）思想沿承**

BERT 统一了两个核心思想：一个是 Transformer 的 Encoder 架构，另一个是预训练+微调的范式。预训练阶段用海量无监督语料学语言能力，微调阶段用少量标注数据适配具体任务。

**（2）模型架构**

BERT 就是把 Transformer 的 Encoder 堆叠起来。输入文本先通过 tokenizer 转成 token id，再经过 Embedding 层变成词向量，然后过 N 层 Encoder，最后接一个分类头输出类别。

BERT 有两个版本：base（12层，768维，1.1亿参数）和 large（24层，1024维，3.4亿参数）。每一层 Encoder 的结构是：先 LayerNorm，再做自注意力（BERT 把相对位置编码融合进了注意力机制），最后过 MLP。BERT 用的是 GELU 激活函数。

**（3）预训练任务——MLM + NSP**

MLM 是掩码语言模型，就是"完形填空"：随机遮住一些 token，让模型根据上下文预测被遮住的词。BERT 随机选 15% 的 token 来遮，但只有 80% 真的遮成 `<MASK>`，10% 换成任意 token，10% 保持不变。这样是为了消除预训练和微调的不一致。

NSP 是下一句预测：给模型两个句子，判断它们是否是连续的上下文。用于训练模型理解句子级关系。

BERT 的预训练用了 33亿 token，训练了 4天（base 用 16 块 TPU，large 用 64 块 TPU）。

**（4）下游任务微调**

预训练好的 BERT 可以直接用在各种 NLU 任务上。输入文本前面会加一个特殊 token `<CLS>`，它代表整句话的语义，用于分类任务。微调时只更新少量参数，效果却很好。BERT 一发布就在 11 个 NLP 任务上取得了 SOTA。

## 三、RoBERTa——BERT 的优化版

RoBERTa 保留了 BERT 的模型架构，但做了三个关键优化。

**（1）去掉 NSP 任务**

实验发现 NSP 任务对模型提升有限，去掉之后效果反而更好。只用 MLM 预训练。

**（2）更大数据和更长训练**

RoBERTa 用了 160GB 的语料（是 BERT 的十倍），包括 BookCorpus、维基百科、CC-NEWS、OPENWEBTEXT 等。同时使用了更大的 batch size（8K），训练了 500K step，全部在 512 长度上训练。

**（3）更大的 BPE 词表**

BERT 用的是 WordPiece 词表（30K），RoBERTa 换成了 BPE 词表（50K），编码效果更好。

RoBERTa 在 BERT 基础上刷新了多个 SOTA，证明了"更大数据、更长训练"的重要性。

## 四、ALBERT——更少的参数，更好的效果

ALBERT 同样基于 BERT 架构，但专注于减少参数量，同时保持或提升模型能力。

**（1）Embedding 参数分解**

BERT 的 Embedding 层参数是 V乘H（词表大小乘隐藏层维度），很大。ALBERT 把 Embedding 层输出设为 128，再通过线性变换升维到 1024，这样参数从 V乘1024 降到了 V乘128 + 128乘1024，大幅减少。

**（2）跨层参数共享**

24 层 Encoder 的参数高度一致。ALBERT 只初始化一层 Encoder，计算时重复用这一层。这样参数量从 340M 降到了 59M，效果反而略好。

缺点是计算量没变少，训练推理速度反而更慢，所以最终没有取代 BERT。

**（3）提出 SOP 预训练任务**

NSP 太简单，模型可以轻易区分正负例，学不到深层语义。SOP（句序预测）把负样本改成两个连续句子顺序颠倒，迫使模型学习更复杂的关系。实验证明 SOP 效果好于 NSP。

## 五、Encoder-Decoder 代表——T5

T5 是 Google 提出的预训练语言模型，全称 Text-To-Text Transfer Transformer。核心思想是把所有 NLP 任务都统一成"文本到文本"的格式：输入一段文字，输出也是一段文字。这样不管是分类、翻译还是问答，都可以用同一个模型框架处理。

**（1）模型架构**

T5 用了完整的 Transformer 结构，既有 Encoder 也有 Decoder。Encoder 负责理解输入文本，Decoder 负责生成输出文本。两者之间通过注意力机制连接，让 Decoder 能看到 Encoder 的编码结果。

T5 的 Encoder 和 Decoder 都是由多个 Block 堆叠而成，每个 Block 包含多头注意力机制、前馈神经网络和 Norm 层。可以用不同数量的 Block 拼出不同规模的模型。

T5 的归一化用的是 RMSNorm，不像 LayerNorm 那样对每个神经元算均值，而是对整个隐藏层算均方根。参数更少，效果差不多。

**（2）预训练任务**

T5 的预训练任务是 MLM，也就是随机遮住 15% 的 token，让模型预测被遮住的部分。这和 BERT 的 MLM 类似。

T5 的预训练数据规模很大，从 Common Crawl 里清洗出了 750GB 的干净英文语料，叫 C4 数据集。

**（3）大一统思想**

T5 最重要的贡献是提出了大一统思想：所有 NLP 任务都可以用同一个框架处理，只需要在输入前面加一个任务前缀就行。

比如：输入"classify: 这是一个很好的产品"，输出"正面"；输入"translate English to French: How are you?"，输出"Comment ça va?"。

这种做法让模型可以在预训练阶段学到不同任务之间的通用特征，也方便在微调时快速适配具体任务。

## 六、总结

BERT 确立了 Encoder-Only 架构和预训练+微调范式，在 NLU 任务上称霸多年。RoBERTa 通过优化训练数据和任务证明了 scale 的重要性。ALBERT 探索了参数共享和结构优化，虽然没成为主流，但为后续模型提供了思路。T5 则用 Encoder-Decoder 架构和文本到文本的统一格式，证明了多任务大一统的可行性。