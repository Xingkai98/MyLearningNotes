# Task2：Agent 心智架构：感知-推理-行动循环

> 来源: Datawhale《Agent设计模式》共学营
> Task2 内容整理

---

## 一、从"工具"到"伙伴"：Agent 范式的本质升级

### 1.1 重新理解 AI 的角色

在传统编程范式中，我们把 AI 模型当作**工具**来使用——给定输入，调用模型，返回结果。这种方式下，人类始终在**循环内部**，掌控着目标设定、任务分解、行动选择的每一个环节。

但 Agent 的出现彻底改变了这一格局。当我们把 AI 模型当作**伙伴**时，它不再只是一个被动响应指令的工具，而是一个能够自主设定子目标、规划行动路线、持续做决策的智能体。这时候，人类移到了**循环外部**，从执行者变成了监督者和目标设定者。

举一个形象的例子：**工具**像是你手中的键盘和鼠标，每一次点击、每一次输入都是你在操控；而**伙伴**则像是你雇用的一位助理，你可以告诉他"帮我整理一下这个月的销售报告"，他就会自主地去收集数据、分析内容、生成结论，你只需要在最后审阅即可。

### 1.2 工具与伙伴的本质区别

| 维度 | 工具（Tools） | 伙伴（Agents） |
|------|---------------|----------------|
| **操作粒度** | 每次调用执行单一操作 | 自主设定子目标、连续决策 |
| **人类位置** | 在循环内部 | 在循环外部 |
| **自主程度** | 完全被动，等待指令 | 主动规划和执行 |
| **适用场景** | 确定性高、步骤简单的任务 | 开放性、复杂多步骤任务 |
| **代表例子** | 键盘、API、单一功能函数 | AI Assistant、AutoGPT |

理解这一区别至关重要。当你在设计一个 AI 应用时，首先需要问自己：我需要的是工具还是伙伴？如果只是需要一个随时待命的执行者，那用普通的 API 调用就足够了；如果需要一个能够自主探索、试错、学习的智能助手，那就要考虑 Agent 的架构设计了。

---

## 二、感知-推理-行动：Agent 的内核循环

### 2.1 循环的工作机制

Agent 的核心是一个持续运转的**感知-推理-行动（Observe-Reason-Act）循环**。这个循环描述了 Agent 如何与外部环境交互、如何处理信息、如何做出决策。

**观察（Observation）**：Agent 从环境中获取信息。这些信息可能是用户的最新提问、工具返回的执行结果、或者外部世界状态的变化。Agent 不会凭空做决策，它的每一个推理都建立在观察到的信息基础之上。

**推理（Reasoning）**：基于观察到的信息，Agent 进行内部思考。这包括理解当前任务的状态、判断下一步应该做什么、评估不同行动的利弊等。推理是 Agent 智能的核心体现。

**行动（Action）**：根据推理的结论，Agent 执行具体的行动。这些行动可能是调用工具、生成文本、修改内部状态，或者向环境发送信息。

这三个环节不是单向流动的，而是形成一个**闭环**：行动会产生新的观察，新的观察会触发新的推理，新的推理又会产生新的行动，如此往复。

### 2.2 为什么这个循环如此重要？

传统 AI 模型是"一次性"的——输入Prompt，输出结果，任务结束。但 Agent 不同，它的价值恰恰在于这个**持续运行的循环**：

- **处理复杂任务**：复杂任务往往不能一步完成，需要多轮交互和迭代改进。Agent 循环可以持续工作直到任务完成。
- **适应环境变化**：现实世界是动态的，Agent 能够根据环境的变化（通过观察获取）实时调整自己的策略。
- **试错与学习**：通过循环中的反馈机制，Agent 可以发现自己之前的错误并纠正，实现自我改进。

理解了这个循环，我们就把握住了 Agent 的本质。无论是 ReAct、Plan 还是 Reflect 范式，它们的核心都是在这个循环的基础上添加不同的策略和机制。

---

## 三、Agent 的三大支柱：记忆、推理与行动

### 3.1 记忆机制：Agent 的经验库

没有记忆的 Agent 就如同没有历史的人，每一个新任务都是从零开始。Agent 的**记忆（Memory）**机制让它能够积累经验、存储知识，从而在后续任务中表现得越来越好。

**短期记忆与长期记忆的区分**：

- **短期记忆（Short-Term Memory）**：相当于工作记忆，存储当前任务的相关信息。比如用户当前提出的问题、对话的上下文、最近几次工具调用的结果等。短期记忆容量有限，通常需要通过摘要等方式压缩信息。
- **长期记忆（Long-Term Memory）**：相当于知识库，存储 Agent 从历史任务中习得的知识和经验。这些经验可以在未来的相似任务中被检索和使用。

**记忆的实现挑战**：

Agent 需要处理多种类型的记忆信息——对话历史、工具使用经验、外部知识等。如何高效存储、快速检索、避免遗忘，是记忆机制设计的核心问题。

**向量数据库**是目前主流的记忆存储方案。它将文本内容转换为向量表示，使得语义相似的内容能够在向量空间中相邻存储。这样，当 Agent 需要检索相关记忆时，可以通过向量相似度快速找到最相关的信息。

**摘要压缩**则是解决 token 成本问题的重要手段。对话历史可能很长，如果全部保留会造成巨大的 token 消耗。通过对历史信息进行摘要压缩，可以在保留关键信息的同时大幅减少 token 使用量。

### 3.2 推理机制：Agent 的思考过程

推理（Reasoning）是 Agent 最核心的能力，它决定了 Agent 如何分析和解决问题。

Agent 的推理有几个关键特征：

- **基于观察**：Agent 的推理不是凭空进行的，而是建立在它观察到的信息基础之上。
- **链式思考**：复杂问题需要多步推理，Agent 能够形成逻辑链，一步步接近答案。
- **可解释性**：通过显式输出推理过程，Agent 的决策是透明可解释的。

推理机制的设计直接影响 Agent 的能力上限。一个好的推理机制让 Agent 能够处理复杂任务、进行有效试错；一个差的推理机制可能导致 Agent 陷入死循环或者做出错误决策。

### 3.3 行动机制：Agent 与世界的交互

行动（Action）是 Agent 将决策转化为结果的过程。没有行动能力的 Agent 只能是"纸上谈兵"。

Agent 的行动类型包括：

- **工具调用**：这是最常见的行动类型。Agent 通过调用各种工具（搜索、计算、代码执行等）来完成任务。
- **信息生成**：生成文本、代码或其他形式的内容。
- **状态更新**：修改 Agent 自身的内部状态，如更新记忆。
- **外部交互**：与外部系统或环境进行交互。

行动的执行会产生结果，这些结果又成为下一轮观察的输入，形成前文提到的感知-推理-行动闭环。

---

## 四、三大 Agent 范式详解

### 4.1 ReAct：思考与行动协同

**ReAct（Reasoning + Acting）** 范式的核心思想是让 Agent 在推理过程中**交替生成思考（Thought）和行动（Action）**。

传统 AI 模型要么只推理不行动（如 Chain-of-Thought），要么直接行动不推理（如直接调用工具）。ReAct 的创新在于把两者结合起来：Agent 在每一步都会先用自然语言描述自己的思考过程，然后基于这个思考选择并执行行动。

**ReAct 的典型流程**：

```
观察：用户问"北京今天的天气如何？"
思考：我需要先获取北京的天气信息，可以调用天气API。
行动：调用天气API查询北京天气
观察：API返回"晴，25度"
思考：获取到了天气信息，现在可以回答用户了。
行动：生成回答"北京今天天气晴朗，气温25度。"
...
```

在这个例子中，**思考**让 Agent 明确"为什么要这么做"，**行动**则是思考的具体执行。交替进行使得 Agent 的推理过程透明可见，也便于人类监督和纠正。

**ReAct 的优势**：
- 推理过程可解释，方便调试和干预
- 思考和行动相互校验，减少盲目行动
- 适用于需要灵活应变的开放性任务

**ReAct 的局限**：
- 交替执行可能增加任务完成时间
- 对于简单任务可能过度复杂化

### 4.2 Plan-Then-Act：先规划后执行

与 ReAct 的"边想边做"不同，**Plan（Planning）** 范式强调**先制定完整计划，再按计划执行**。

**Plan 范式的核心流程**：

```
第一步（观察）：理解任务目标
第二步（规划）：制定详细的执行计划
第三步（执行）：严格按照计划逐步执行
第四步（完成）：汇总结果，完成任务
```

Plan 范式将**规划阶段**和**执行阶段**明确分开。在规划阶段，Agent 会全面分析任务、拆解步骤、预判可能出现的问题，制定出一个完整的行动方案。只有在规划完成后，才会进入执行阶段，按照计划逐步推进。

这种范式特别适合**路径明确、步骤清晰**的复杂任务。比如"帮我写一个爬虫程序爬取某网站数据"这类任务，Agent 可以先规划出"分析网站结构→编写爬虫代码→测试运行→修复问题→完成数据获取"的步骤，然后按部就班执行。

**Plan 的优势**：
- 结构清晰，执行过程可控可预测
- 计划可以提前检查，发现问题可及时修正
- 便于人类在执行前进行审核和干预

**Plan 的局限**：
- 现实世界往往充满意外，计划可能需要动态调整
- 对于开放性、探索性任务，难以提前制定完整计划

### 4.3 Reflect：行动后的反思与改进

**Reflect（Reflection）** 范式的核心是引入**反思（Reflection）**机制，让 Agent 在行动后主动审视自己的思维过程和行动结果，从经验中学习。

如果说 ReAct 是在行动前思考，Plan 是在行动前规划，那么 Reflect 就是在**行动后反思**。

**Reflect 的典型流程**：

```
观察 → 推理 → 行动 → 反思 → 改进 → 观察 → ...
```

**反思的内容**包括：
- **结果审视**：当前行动的结果是否符合预期？
- **过程审视**：执行过程中是否有可以优化的地方？
- **策略审视**：整体策略是否正确？是否需要调整方向？

**Reflect 的独特价值**在于它的**双向反馈机制**：
- **外部反馈**：来自环境的反馈，如工具返回的错误信息
- **内部反馈**：Agent 对自身思维过程的审视和评判

这种自我审视能力让 Agent 能够进行**持续自我改进**。随着执行的任务越多，Agent 积累的经验越丰富，后续的表现通常会越来越好。

### 4.4 三大范式的对比与选择

| 范式 | 核心机制 | 最大优势 | 主要局限 | 最佳适用场景 |
|------|----------|----------|----------|--------------|
| **ReAct** | 思考与行动交替 | 灵活适应，可解释 | 可能陷入循环 | 开放性任务、需要灵活应变的场景 |
| **Plan** | 先规划后执行 | 结构清晰，可控性强 | 缺乏灵活性 | 路径明确的多步骤任务 |
| **Reflect** | 行动后反思改进 | 持续自我进化 | 需要多次交互 | 需要持续优化、长期运行的任务 |

**如何选择**：

- **任务目标明确、步骤清晰** → 选择 Plan，先规划再执行
- **任务开放性强、不确定因素多** → 选择 ReAct，边想边做
- **任务需要长期运行、持续改进** → 选择 Reflect，从经验中学习

实际应用中，这三种范式并非互斥的。ReAct 可以加入 Plan 的规划能力，Reflect 可以在内部使用 ReAct 的思考方式。关键是理解每种范式的核心思想，然后根据具体需求灵活组合。

---

## 五、记忆机制深入理解

### 5.1 为什么 Agent 需要记忆？

我们人类之所以能够完成复杂任务，很大程度上依赖于记忆。我们记得之前学过的知识、记得上次解决问题的经验、记得用户的偏好和习惯。Agent 也需要类似的记忆能力，否则它每次处理任务都像是"失忆症患者"，无法利用任何历史积累。

举一个例子：用户让 Agent 帮助调试一段代码。如果 Agent 没有记忆，它可能每次都犯同样的错误，每次都需要用户重新解释上下文。但如果 Agent 有记忆，它能够记住之前的调试尝试、记住用户指出的问题，在后续工作中避免重复错误。

### 5.2 短期记忆的实现

短期记忆主要处理**当前任务上下文**。它包括：
- 当前的对话内容
- 当前正在处理的问题描述
- 最近几次工具调用的结果
- 当前任务的中间状态

短期记忆的挑战在于**容量管理**。大模型的上下文窗口是有限的（即使是最新的模型也有 token 上限），不可能无限承载所有信息。

常见的解决方案是**摘要压缩**：当对话历史太长时，Agent 会自动对历史信息进行摘要，提取关键信息，丢弃冗余细节。这类似于人类记忆中的"压缩"机制——我们不会记住对话的每一个字，而是记住核心要点。

### 5.3 长期记忆的实现

长期记忆存储**跨任务的通用知识和经验**。它的来源包括：
- Agent 在历史任务中学习到的知识和技能
- 通用领域知识（可以来自外部知识库）
- 用户的偏好和习惯信息

**向量数据库**是长期记忆的主流存储方案。其工作原理是：
1. 将文本内容通过 Embedding 模型转换为向量
2. 相似的文本在向量空间中距离更近
3. 检索时，将检索内容也转换为向量，找出最相似的记忆

这种方案的优势在于**语义检索**——即使记忆的表述与检索 query 不完全相同，只要语义相近，就能被正确检索到。

---

## 六、本章小结

### 核心概念回顾

1. **Agent 范式**：从"工具"到"伙伴"的升级，人类从循环内部移到外部，Agent 具有自主设定子目标、选择行动的能力。

2. **感知-推理-行动循环**：Agent 运行的核心机制，包括从环境获取观察、基于观察进行推理、执行推理得出的行动三个环节，形成持续运转的闭环。

3. **三大组件**：
   - 记忆：积累经验和知识，分短期记忆和长期记忆
   - 推理：生成思考和决策，是 Agent 智能的核心
   - 行动：执行具体操作，与环境交互

4. **三大范式**：
   - **ReAct**：思考与行动协同交替，适合开放性任务
   - **Plan**：先规划后执行，适合结构清晰的多步骤任务
   - **Reflect**：行动后反思改进，适合需要持续优化的任务

### 学习思考

- 工具和伙伴的边界在哪里？什么情况下应该用工具思维？什么情况下应该用伙伴思维？
- 记忆机制对 Agent 能力的影响是什么？如果记忆质量不高，Agent 可能出现什么问题？
- 三种范式是否可以组合使用？尝试设计一个结合 ReAct 和 Plan 优点的混合范式。

---

## 相关资源

- 共学营其他任务：Task1（Agent本质：从工具到伙伴）、Task3（多Agent系统的编排）、Task4（人机协作的设计原则）、Task5（总结复盘）
- 书籍推荐：《Agent设计模式》https://item.jd.com/15313246.html
