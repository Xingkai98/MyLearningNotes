# 注意力机制：让模型学会"看哪里"

> 学习来源：Datawhale Happy-LLM 第二章 2.1 节
> 个人整理 by Claude

---

## 出发点和困惑

说实话，第一次看到 Query、Key、Value 这三个词的时候我是懵的——查个字典为什么要搞得这么复杂？又不是什么高深的概念。

后来想明白了，这个比喻其实挺好的，但得慢慢品。

---

## 从"傻子才这么做"到"原来如此"

### 一个生活中的例子

想象你要在一堆水果里找到"水果"这个概念对应的值。

字典里只有：
```json
{
    "apple": 10,
    "banana": 5,
    "chair": 2
}
```

**笨方法**：精确匹配——查"水果"？没有，匹配不到。

**聪明方法**：软匹配——"水果"跟 apple 和 banana 都沾边，但跟 chair 完全不搭。所以：

```json
{
    "apple": 0.6,
    "banana": 0.4,
    "chair": 0
}
```

最终值 = 0.6×10 + 0.4×5 + 0×2 = 8

这个"0.6、0.4、0"就是**注意力分数**——它反映了我们应该把注意力往哪儿放。

### Query、Key、Value 到底是什么？

回到那三个词，我的理解是这样的：

- **Query（查询）**：你心里想问的问题。比如"水果值多少"
- **Key（键）**：字典里每个词的名字。apple、banana、chair
- **Value（值）**：字典里每个词对应的实际数值。10、5、2

Query 和 Key 做匹配，得到注意力权重，再用权重去加权 Value——这就是注意力机制的本质。

---

## 公式是怎么来的

### 第一步：找出相似度

用什么衡量 Query 和 Key 像不像？**点积**。

```
q · k = Σ(qi × ki)
```

为什么是点积？因为词向量有个特性：语义相近的词，向量点积更大。

### 第二步：转成概率分布

光有点积不行，数值有大有小，不方便比较。扔给 Softmax：

```
softmax(xi) = e^xi / Σe^xj
```

输出的东西相加等于 1，正好表示"注意力权重"。

### 第三步：加权求和

用注意力权重去加权 Value：

```
attention(Q, K, V) = softmax(QK^T) × V
```

### 第四步：缩放一下

当 dk（Key 的维度）比较大的时候，QK^T 的值会偏大，Softmax 容易走到极端区域，梯度不稳定。

解决：除以 √dk

```
attention(Q, K, V) = softmax(QK^T / √dk) × V
```

这就是最终的公式。

---

## 自注意力：自己看自己

普通的注意力是"两个序列之间"的注意力——Query 来自 A，Key/Value 来自 B。

但 Transformer 的 Encoder 用的是**自注意力**——Query、Key、Value 都来自同一个序列。

说白了就是：我这句话里的每个词，跟这句话里其他词的关系是什么样的？

代码就一行：
```python
attention(x, x, x)  # 输入三遍 x
```

---

## 掩码自注意力：为什么不能偷看答案

这里有个很妙的设计。

训练的时候，语言模型要做的是：根据前文预测下一个词。

正常做法是串行的——先算第一个，再算第二个，再算第三个...

但这样太慢了，没利用上 GPU 的并行能力。

**Mask 的思路**：既然第 4 个位置不该看到第 5、6、7... 个词，那就在注意力计算时把这些位置"遮住"。

怎么遮？用 -inf。

```
# 上三角的位置全是 -inf
scores = scores + mask  # -inf 的地方加完还是 -inf
scores = softmax(scores)  # softmax(-inf) = 0
```

被 mask 住的位置，注意力分数变成 0，相当于模型"假装那些 token 不存在"。

这样就能一次把整个序列扔进模型，让模型同时学预测所有位置的下一个词——并行训练，效率拉满。

---

## 多头注意力：一个问题有多个角度

单一注意力只能学到一种关系。

但一句话里可能同时存在：
- 主语和谓语的关系
- 指代关系（"他"指谁）
- 语义相似关系
- 语法结构关系
- ...

**多头注意力**就是同时运行多组 (Wq, Wk, Wv)，每组学一种关系，最后把结果拼起来：

```
MultiHead(Q, K, V) = Concat(head_1, ..., head_h) × Wo
where head_i = Attention(QWq_i, KWk_i, VWv_i)
```

类比一下：就像你分析一篇文章时，既要看段落大意（一种注意力），又要注意修辞手法（另一种注意力），还要理解作者意图（第三种注意力）——最后综合起来理解全文。

---

## 整体回顾

注意力机制说起来复杂，但逻辑很清晰：

1. **为了效率**：让模型知道该"关注"哪些信息
2. **通过相似度**：用 Query 和 Key 的点积衡量相关性
3. **加权求和**：用注意力权重对 Value 加权
4. **自注意力**：让序列自己跟自己玩，建模内部依赖
5. **掩码**：防止看到未来信息，保持因果性
6. **多头**：从多个角度同时建模，学到更丰富的关系

说到底，注意力机制解决的核心问题是：**如何让模型在处理一个序列时，知道序列里哪些部分跟哪些部分有关系，关系有多强。**

这个问题，RNN 靠"依次读取"来解决（慢，且长距离关系容易丢），注意力机制靠"并行查询"来解决（快，且能直接建模任意距离的关系）。

难怪它能取代 RNN 成为 NLP 的主流架构。

---

## 参考

- Vaswani et al. (2017). *Attention Is All You Need.*
- Jay Mody. *An Intuition for Attention.*
